# 希希出行 — AI 智能旅游规划平台（全栈视角）

## 项目概述

基于 Vue 3 + Express + LangChain 的 AI 旅游规划平台，前后端独立开发，实现 LLM 流式对话、结构化行程推荐、天气查询、用户认证等完整功能。

## 技术栈

- 前端：Vue 3 + TypeScript + Vant 4 + Vue Router + Axios + Vite
- 后端：Node.js + Express 5 + LangChain + @langchain/openai
- 通信：RESTful API + SSE（Server-Sent Events）流式推送
- 外部服务：传智播客 API（认证/天气/地区）、SiliconFlow / DeepSeek（LLM）

---

## 前端视角

### [💡 全链路流式通信架构] 基于 SSE + ReadableStream 实现 LLM 流式对话，封装可复用的 fetchStream 工具函数，支持 AbortController 中断

**情境**：LLM 生成响应需要数十秒，传统"请求-等待-完整响应"模式用户体验差。
**任务**：实现从 LLM 到前端 UI 的实时流式数据传输，让用户看到 AI 逐字"打字"效果。
**行动**：
- 后端：封装 `createStreamResponse` 工具函数，统一 SSE 协议头（`text/event-stream`、`no-cache`、`keep-alive`），暴露 `send/end/error` 三个语义化方法；路由层通过回调模式接收 LLM chunk，以 `{type:"chunk",data:"..."}` 的 JSON 格式写入 SSE 流
- 前端：封装 `fetchStream` 函数，基于 `fetch` + `ReadableStream.getReader()` + `TextDecoder` 消费 SSE 事件流，按行解析 `data:` 前缀，支持 `chunk`/`end`/`error` 三种消息类型，兼容 `[DONE]` 结束标记和纯文本兜底
- 安全：路由层三重 `res.writableEnded` 防护防止连接断开后写入崩溃；前端 `AbortController` 支持用户主动取消流式请求
**结果**：用户输入后即时看到 AI 逐字回复，交互体验接近 ChatGPT。

**源码实证**：
- 后端 `createStreamResponse`：[createStreamResponse.js](file:///c:/Users/林希/Desktop/ai旅游项目/Backend/src/utils/createStreamResponse.js)
- 后端流式路由调度：[travel.js#L19-L53](file:///c:/Users/林希/Desktop/ai旅游项目/Backend/src/routes/travel.js#L19-L53)
- 后端 Service 层流式迭代：[traelService.js#L69-L98](file:///c:/Users/林希/Desktop/ai旅游项目/Backend/src/services/traelService.js#L69-L98)
- 前端 fetchStream 封装：[request.ts#L55-L159](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/utils/request.ts#L55-L159)
- 前端 ChartView 流式渲染：[ChartView.vue#L115-L157](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/views/ChartView.vue#L115-L157)

---

### [💡 结构化 Prompt 工程与容错解析] 设计 JSON Schema 约束 Prompt + 三层正则兜底解析，将 LLM 非结构化文本可靠转化为前端可渲染的结构化行程数据

**情境**：LLM 输出本质是非结构化文本，直接 `JSON.parse` 极易失败，无法在前端渲染。
**任务**：让 LLM 稳定输出结构化 JSON 数据，并在 LLM 格式不严格时仍能正确解析。
**行动**：
- Prompt 层：在提示词中嵌入完整 JSON Schema 示例（含 `dailyItinerary`、`budgetBreakdown`、`tips`、`warnings`），明确约束 LLM 严格按格式输出，禁止输出解释性寒暄
- 解析层：三层正则兜底策略，按优先级匹配 ` ```json ``` ` 代码块 → ` ``` ``` ` 无标记代码块 → 裸 `{...}` 对象，逐层降级保障解析成功率
**结果**：LLM 返回的任意格式均能可靠解析，前端行程详情页正确渲染每日上/下午/晚上行程、预算明细、温馨提示。

**源码实证**：
- Prompt 模板：[traelService.js#L101-L175](file:///c:/Users/林希/Desktop/ai旅游项目/Backend/src/services/traelService.js#L101-L175)
- 三层正则解析：[traelService.js#L47-L50](file:///c:/Users/林希/Desktop/ai旅游项目/Backend/src/services/traelService.js#L47-L50)
- 前端详情页渲染：[detailView.vue](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/views/detailView.vue)

---

### [💡 前端认证全链路] 实现路由守卫 + Axios 拦截器 + 401 自动登出的完整认证闭环，双客户端实例隔离内部 API 与第三方 API 的认证逻辑

**情境**：应用涉及两类 API — 自有后端（无需认证）和第三方传智 API（需 Bearer Token），需要统一的认证状态管理。
**任务**：建立从登录态判断到请求注入再到异常处理的完整认证链路。
**行动**：
- 创建两个独立的 Axios 实例：`request`（指向自有后端 `localhost:4000`）和 `itheimaApi`（指向传智 API `hmajax.itheima.net`），各自独立配置拦截器
- `itheimaApi` 请求拦截器自动从 localStorage 读取 Token 注入 `Authorization` 头
- 响应拦截器检测 401 状态码，自动清除 localStorage 并跳转登录页
- 路由守卫 `beforeEach`：`requiresAuth` 页面未登录重定向 `/auth`，已登录用户访问 `/auth` 重定向首页
- 登录信息持久化到 localStorage（`travel_token`、`travel_user_info`、`travel_is_logged_in`），刷新不丢失
**结果**：用户登录态在路由跳转、页面刷新、API 请求三个维度保持一致，Token 过期自动引导重新登录。

**源码实证**：
- 路由守卫：[router/index.ts#L47-L66](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/router/index.ts#L47-L66)
- 双客户端实例 + Token 注入：[itheima-api.ts](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/utils/itheima-api.ts)
- 认证工具函数：[auth.ts](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/utils/auth.ts)
- 自有后端客户端（含 CancelToken）：[request.ts#L1-L48](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/utils/request.ts#L1-L48)

---

### [💡 请求取消与防抖机制] 封装 CancelToken 可取消请求 + lodash debounce 防抖，防止页面切换导致的内存泄漏与重复请求

**情境**：AI 推荐接口耗时较长（5-30分钟超时），用户可能在等待期间返回上一页，残留请求继续占用资源。
**任务**：让组件卸载时能主动取消进行中的请求，避免无效请求和内存泄漏。
**行动**：
- 重写 `request.post`：每次调用创建 `axios.CancelToken.source()`，Promise 挂载 `.cancel()` 方法
- 详情页 `onBack` 时调用 `currentRequest.cancel()` 取消进行中的推荐请求
- 推荐请求做 300ms `debounce` 防抖，避免快速点击触发重复请求
**结果**：页面返回时立即取消进行中的请求，不会出现"页面已离开但响应才到达"的内存泄漏问题。

**源码实证**：
- CancelToken 封装：[request.ts#L34-L48](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/utils/request.ts#L34-L48)
- 详情页取消逻辑：[detailView.vue#L160-L161](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/views/detailView.vue#L160-L161) + [detailView.vue#L261-L268](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/views/detailView.vue#L261-L268)

---

## 全栈 / 后端视角

### [💡 LLM 多 Provider 可切换架构] 基于环境变量实现多 LLM 服务商配置切换，统一通过 @langchain/openai 的 ChatOpenAI 适配 OpenAI 兼容协议

**情境**：不同 LLM 服务商（SiliconFlow、DeepSeek）的 API Key、Base URL、模型名各不相同，但都兼容 OpenAI 接口协议。
**任务**：实现一套代码适配多个 LLM Provider，通过配置切换而非代码改动。
**行动**：
- 在 `TravelService.initLLM()` 中通过 `MODEL_PROVIDER` 环境变量判断当前 Provider
- 根据 Provider 读取对应的 `API_KEY`、`BASE_URL`、`MODEL` 环境变量
- 统一注入 `ChatOpenAI` 构造函数，通过 `configuration.baseURL` 指向不同服务商地址
- 启用 `streaming: true` 以支持后续流式对话
**结果**：修改 `.env` 中 `MODEL_PROVIDER` 值并重启服务即可切换 LLM 服务商，无需改动业务代码。

**源码实证**：
- Provider 切换逻辑：[traelService.js#L11-L31](file:///c:/Users/林希/Desktop/ai旅游项目/Backend/src/services/traelService.js#L11-L31)

---

### [💡 Express 流式响应路由设计] 采用回调模式解耦 Service 层与路由层，通过 try/catch/finally + writableEnded 三重防护确保 SSE 连接安全关闭

**情境**：SSE 长连接场景下，任何环节异常（LLM 调用失败、网络中断、客户端断开）都可能导致连接泄漏或写入崩溃。
**任务**：设计健壮的路由层流式调度逻辑，确保无论成功/失败/异常，连接都能安全关闭。
**行动**：
- Service 层通过回调模式（`streamCallback`）将 token 逐块传出，不直接操作 Response 对象，保持 Service 层与 HTTP 层解耦
- 路由层 try 块中处理正常流式推送，catch 块中通过 `streamResponse.error()` 发送错误信息，finally 块中统一调用 `streamResponse.end()` 关闭连接
- 每次写入前检查 `res.writableEnded`，避免连接已断开时写入导致 `ERR_STREAM_WRITE_AFTER_END` 崩溃
- `createStreamResponse` 内部所有方法均有 try-catch 保护
**结果**：流式聊天在正常、异常、超时、客户端断开等场景下均不会崩溃或泄漏连接。

**源码实证**：
- 路由层流式调度：[travel.js#L19-L53](file:///c:/Users/林希/Desktop/ai旅游项目/Backend/src/routes/travel.js#L19-L53)
- SSE 工具函数：[createStreamResponse.js](file:///c:/Users/林希/Desktop/ai旅游项目/Backend/src/utils/createStreamResponse.js)

---

### [💡 前后端分离 + 双 API 源聚合] 前端同时对接自有后端与第三方 API，通过双 Axios 实例 + 统一错误处理实现 API 层的关注点分离

**情境**：项目需要同时调用自有后端（LLM 推荐/聊天）和第三方传智 API（认证/天气/地区/个人信息），两类 API 的 baseURL、认证方式、响应格式完全不同。
**任务**：设计清晰的 API 层架构，避免两类 API 的配置和逻辑耦合。
**行动**：
- 创建两个独立 Axios 实例：`request`（`localhost:4000`，无 Token 注入）和 `itheimaApi`（`hmajax.itheima.net`，自动注入 Bearer Token）
- `itheimaApi` 响应拦截器统一处理业务错误码（`code !== 10000`）和 401 过期
- 按业务域封装 API 模块：`auth.ts`（认证）、`weather.ts`（天气）、`profile.ts`（个人信息）、`location.ts`（地区搜索）
- 每个模块从 `itheimaApi` 实例发出请求，享受统一的 Token 注入和错误处理
- 自有后端的流式请求单独走 `fetchStream` 函数，不经过 Axios，因为 Axios 不支持 ReadableStream 消费
**结果**：API 层职责清晰，新增第三方 API 只需新建模块文件，认证和错误处理逻辑零重复。

**源码实证**：
- 双 Axios 实例定义：[request.ts#L1-L48](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/utils/request.ts#L1-L48) + [itheima-api.ts](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/utils/itheima-api.ts)
- API 模块：`auth.ts`、`weather.ts`、`profile.ts`、`location.ts`
- 流式请求独立实现：[request.ts#L55-L159](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/utils/request.ts#L55-L159)

---

## 项目规模

- 前端：7 个页面、6 个组件、6 个工具模块，约 2500 行
- 后端：4 个模块（路由/服务/工具/入口），约 280 行
- 接口：2 个 RESTful 端点 + 1 个 SSE 流式端点 + 1 个心跳端点