# 希希出行 — 面试技术亮点总结

---

## 一、项目一句话介绍

"AI 智能旅游规划平台，用户输入城市/预算/天数，AI 自动生成结构化行程计划（每日上下午晚安排 + 预算明细 + 注意事项），同时支持流式 AI 对话。前端 Vue 3 + TypeScript + Vant 4，后端 Express 5 + LangChain，全栈独立开发，7 个页面 6 个组件 10 个工具模块。"

---

## 二、前端技术亮点

### 亮点 1：组件化架构设计 — 6 个高复用业务组件，单一职责 + Props 类型约束

**做了什么**：
- 将行程详情页拆分为 `SpotItem`（景点卡片）、`BudgetTable`（预算明细表）、`WeatherCard`（天气卡片）三个独立组件
- 将聊天页拆出 `ChatBubble`（聊天气泡）组件，通过 `role` prop 区分用户/AI 样式
- 每个组件使用 `defineProps` 接收数据，内部通过 `computed` 派生 UI 状态，不直接修改 props

**代码实证**：
- [SpotItem.vue](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/componets/SpotItem.vue) — 接收 `data` Object，内部用 `v-if` 按需渲染 duration/ticket/transportation/description 四个字段，无数据时显示 `<van-empty>`
- [BudgetTable.vue](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/componets/BudgetTable.vue) — 接收 `data` Object + `total`，通过 `computed` 将原始字段映射为中文标签（accommodation→住宿），展示合计金额
- [ChatBubble.vue](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/componets/ChatBubble.vue) — 接收 `message` Object，`computed` 派生 `messageClass`（user-message / ai-message）和格式化时间
- [WeatherCard.vue](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/components/WeatherCard.vue) — 接收 `cityName` + `weatherData`，`computed` 根据 PM2.5 等级动态计算 CSS 类名（level-good / level-medium / level-bad）

**面试话术**：
"我把页面中的可复用 UI 块抽成了独立组件，每个组件只通过 props 接收数据，内部用 computed 做派生状态，不直接修改父组件数据。比如 WeatherCard 根据 PM2.5 等级自动切换文字颜色，SpotItem 用 v-if 按需渲染字段，数据为空时优雅降级显示 empty 状态。"

---

### 亮点 2：路由懒加载 + 路由守卫 + Tabbar 条件渲染

**做了什么**：
- 全部 7 个路由使用 `() => import()` 动态导入，Vite 自动代码分割，首屏只加载当前页面
- `beforeEach` 路由守卫实现三级判断逻辑：需要认证 → 未登录跳转 `/auth`；访问 `/auth` → 已登录重定向首页；公开页面直接放行
- `App.vue` 通过 `computed` 监听当前路由，登录页不显示底部 Tabbar（`v-if="showTabbar"`）

**代码实证**：
- 路由懒加载：[router/index.ts#L4-L40](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/router/index.ts#L4-L40) — 7 条路由全部 `component: () => import(...)`
- 路由守卫：[router/index.ts#L47-L66](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/router/index.ts#L47-L66) — 三重 if-else 判断
- Tabbar 条件渲染：[App.vue#L3-L9](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/App.vue#L3-L9) — `showTabbar = computed(() => route.path !== '/auth')`

**面试话术**：
"全部路由用了动态 import 做代码分割，首屏只加载首页代码，其他页面按需加载。路由守卫做了三个分支：需要登录的页面拦截、已登录用户不能重复访问登录页、公开页面直接放行。另外 App.vue 监听路由，登录页隐藏底部导航栏，其他页面显示，这个细节让 UI 更合理。"

---

### 亮点 3：双 Axios 实例架构 — 解耦自有后端与第三方 API 的认证/错误处理

**做了什么**：
- 创建两个独立的 Axios 实例：`request`（`localhost:4000`，无认证拦截）和 `itheimaApi`（`hmajax.itheima.net`，自动注入 Bearer Token）
- 每个实例独立配置 `baseURL`、`timeout`、拦截器，互不污染
- `itheimaApi` 请求拦截器从 localStorage 读 Token 注入 `Authorization` 头；响应拦截器统一处理业务错误码和 401 过期
- 按业务域拆分 4 个 API 模块：`auth.ts`、`weather.ts`、`profile.ts`、`location.ts`，全部从 `itheimaApi` 实例发请求
- 流式请求单独走 `fetchStream` 函数（原生 fetch），因为 Axios 不支持 `ReadableStream` 消费

**代码实证**：
- `request` 实例：[request.ts#L1-L48](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/utils/request.ts#L1-L48)
- `itheimaApi` 实例（含 Token 注入 + 401 处理）：[itheima-api.ts](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/utils/itheima-api.ts)
- API 模块文件结构：`auth.ts`、`weather.ts`、`profile.ts`、`location.ts`

**面试话术**：
"项目里有两套 API：我自己的后端负责 LLM 推荐和聊天，第三方传智 API 负责认证、天气、个人信息。我不想让两套 API 的 baseURL、认证逻辑、错误处理搅在一起，所以创建了两个独立的 Axios 实例，每个实例有自己的拦截器。第三方 API 的 Token 注入和 401 处理只挂在 itheimaApi 上，不会影响自有后端。另外流式请求我单独用了 fetch，因为 Axios 不支持消费 ReadableStream。"

---

### 亮点 4：请求取消（CancelToken）+ 防抖（debounce）— 页面切换时的资源清理

**做了什么**：
- 重写 `request.post`：每次调用创建 `axios.CancelToken.source()`，返回的 Promise 上挂载 `.cancel()` 方法
- 详情页在 `onBack` 时调用 `currentRequest.cancel()` 取消进行中的推荐请求，防止"页面已返回但请求才完成"导致的状态更新到已卸载组件
- 推荐请求包裹在 `debounce(fetchRecommend, 300)` 中，防止快速重复点击
- catch 块中过滤取消错误（`err.message !== '请求已被取消'`），取消操作不显示错误提示

**代码实证**：
- CancelToken 封装：[request.ts#L34-L48](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/utils/request.ts#L34-L48)
- 详情页取消逻辑：[detailView.vue#L263-L268](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/views/detailView.vue#L263-L268)
- 防抖 + 取消错误过滤：[detailView.vue#L250-L261](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/views/detailView.vue#L250-L261)

**面试话术**：
"AI 推荐接口可能耗时很长（timeout 设了 5 分钟），如果用户等不及返回上一页，浏览器不会自动取消这个请求，响应回来后还会去更新已卸载的组件。我重写了 axios.post，每次请求创建 CancelToken，Promise 上挂一个 cancel 方法。onBack 时调用 cancel，并在 catch 里过滤掉取消错误，不显示无意义的报错。再加上 300ms 防抖，避免快速点击发重复请求。"

---

### 亮点 5：聊天页流式渲染 + 自动滚动 + 常见问题快捷输入

**做了什么**：
- `fetchStream` 封装了 SSE 消费全流程：`fetch` → `response.body.getReader()` → `while(true)` 循环读取 → `TextDecoder` 解码 → 按行解析 `data:` 前缀 → JSON.parse 分包
- 流式数据到达后追加到消息数组最后一条 AI 消息的 `content`，配合 `nextTick` + `scrollTo` 自动滚动到底部
- 聊天输入区在 AI 回复期间（`isStreaming`）禁用发送按钮和输入框
- 空状态时展示常见问题标签，点击直接发送；从详情页跳转时自动填入 "我想了解XX的旅游信息"

**代码实证**：
- fetchStream 封装：[request.ts#L55-L159](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/utils/request.ts#L55-L159)
- 聊天页流式渲染：[ChartView.vue#L115-L157](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/views/ChartView.vue#L115-L157)
- 自动滚动：[ChartView.vue#L78-L84](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/views/ChartView.vue#L78-L84)
- 详情页跳转自动填入：[ChartView.vue#L49-L52](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/views/ChartView.vue#L49-L52)

**面试话术**：
"聊天页的核心是流式渲染。我封装了 fetchStream 函数，用 fetch + ReadableStream 消费 SSE 事件流，每收到一个 chunk 就追加到 Vue 消息数组里最后一条 AI 消息上。同时配合 nextTick 做自动滚动到底部。流式回复期间禁用输入框防止误操作。还有个细节是支持从详情页跳转过来时自动填入城市信息，减少用户输入。"

---

### 亮点 6：多状态 UI 覆盖 — Loading / Empty / Error / 正常渲染四种状态全处理

**做了什么**：
- 详情页用 `v-if / v-else-if / v-else` 覆盖四种状态：Loading（van-loading 旋转动画 + "正在生成旅游规划" 文字）→ Error（van-empty + 重试按钮）→ 正常渲染（折叠面板行程 + 预算明细 + 温馨提示 + 注意事项）→ 空数据（暂无行程数据）
- 聊天页空状态展示引导文案 + 常见问题快捷标签；非空状态展示消息列表 + 流式加载指示器
- 首页搜索弹窗：有搜索结果展示列表，无结果展示 van-empty

**代码实证**：
- 详情页多状态：[detailView.vue#L7-L116](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/views/detailView.vue#L7-L116)
- 聊天页空状态：[ChartView.vue#L8-L17](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/views/ChartView.vue#L8-L17)
- 首页搜索弹窗：[HomeView.vue#L49-L56](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/views/HomeView.vue#L49-L56)

**面试话术**：
"前端开发最容易忽略的就是各种边界状态。我每个页面都覆盖了 Loading、Empty、Error 三种非正常状态。比如详情页用 van-loading 展示生成中状态，API 失败展示 van-empty + 重试按钮，数据为空提示暂无行程。这在实际产品中直接影响用户体验。"

---

### 亮点 7：天气搜索防抖 + 下拉动画 + 三级行政区划搜索

**做了什么**：
- WeatherView 搜索输入后 300ms setTimeout 防抖，避免每次按键都发请求
- 搜索结果下拉框用 CSS transition（opacity + visibility + transform）实现平滑出现/消失动画
- `onBlur` 延迟 300ms 隐藏下拉框，确保点击结果项时下拉框还没消失
- `location.ts` 封装了 "搜索省份 → 搜索城市 → 搜索区县" 三级联动，用 `Promise.all` 并行请求三级数据，合并后截取前 20 条

**代码实证**：
- 防抖搜索：[WeatherView.vue#L88-L112](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/views/WeatherView.vue#L88-L112)
- 下拉动画 CSS：[WeatherView.vue#L189-L197](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/views/WeatherView.vue#L189-L197)
- 三级并行搜索：[location.ts#L72-L82](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/src/utils/location.ts#L72-L82)

**面试话术**：
"天气搜索页做了 300ms 防抖，避免用户每敲一个字都发一次 API。搜索结果下拉框用了 CSS transition 做动画，不是生硬的显示隐藏。还有一个点是三级行政区划搜索——省份、城市、区县——我用 Promise.all 并行搜三级，然后合并结果截取前 20 条，比串行搜索快很多。"

---

### 亮点 8：工程化规范 — ESLint + Prettier + oxlint + TypeScript + 路径别名

**做了什么**：
- ESLint 配置了 Vue + TypeScript 规则集 + oxlint（Rust 写的超快 Linter）
- 通过 `eslint-config-prettier/flat` 禁用与 Prettier 冲突的规则
- Prettier 配置：无分号、单引号、100 字符换行
- TypeScript 严格模式：所有 API 返回值和组件 props 都有 interface 类型定义
- Vite 配置 `@` 路径别名指向 `src/`，避免 `../../../` 深层相对路径
- 自定义 ESLint 规则：关闭 `vue/multi-word-component-names`（允许单单词组件名如 Auth、Home），关闭 `no-explicit-any` 等 TS 严格限制

**代码实证**：
- ESLint 配置：[eslint.config.ts](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/eslint.config.ts)
- Prettier 配置：[.prettierrc.json](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/.prettierrc.json)
- Vite 路径别名：[vite.config.ts#L13-L16](file:///c:/Users/林希/Desktop/ai旅游项目/vue-ai-frontend-travel/vite.config.ts#L13-L16)
- TypeScript 接口定义：`weather.ts` 中 `CityInfo`、`WeatherData`、`TodayWeather`、`DayForecast`；`auth.ts` 中 `UserInfo`；`profile.ts` 中 `ProfileData`

**面试话术**：
"项目配了完整的工程化规范：ESLint + oxlint 做代码检查，Prettier 统一格式，TypeScript 做类型约束。特别是接口层，所有 API 返回值都定义了 interface，比如天气数据有 WeatherData、TodayWeather、DayForecast 三层嵌套类型，子组件 Props 也用 TypeScript 约束。Vite 配了 @ 路径别名，避免深层相对路径。"

---

## 三、后端技术亮点

### 亮点 1：SSE 流式响应工具封装 — 协议细节屏蔽 + 异常安全三层防护

**做了什么**：
- 封装 `createStreamResponse(res)` 函数，内部统一设置 SSE 三个必需响应头（`Content-Type: text/event-stream`、`Cache-Control: no-cache`、`Connection: keep-alive`）
- 返回 `{ send, end, error }` 三个语义化方法，内部自动拼接 `data: xxx\n\n` 的 SSE 协议格式
- 每个方法内部 try-catch 保护，防止写入失败导致进程崩溃
- `end()` 方法在调用 `res.end()` 前先发送 `data: end\n\n` 标记

**代码实证**：[createStreamResponse.js](file:///c:/Users/林希/Desktop/ai旅游项目/Backend/src/utils/createStreamResponse.js)

**面试话术**：
"SSE 协议有几个细节很容易出错：必须设置三个特定响应头，数据格式必须是 `data: xxx\n\n`（两个换行）。我把这些细节封装成 createStreamResponse 工具函数，暴露 send/end/error 三个方法。路由层调用时不用关心协议格式，只需要在回调里调用 send 把数据推出去。每个方法内部都有 try-catch，防止写入异常把整个进程搞崩。"

---

### 亮点 2：回调模式解耦 + try/catch/finally 三段式流式路由

**做了什么**：
- Service 层的 `chat` 方法不直接操作 Express 的 `res` 对象，而是通过 `streamCallback` 回调函数逐 chunk 传出
- 路由层通过 try/catch/finally 三段式结构处理流式生命周期：
  - try：正常推送 chunk + 发送结束标记
  - catch：调用 `streamResponse.error()` 发送错误信息
  - finally：统一调用 `streamResponse.end()` 关闭 SSE 连接
- 每次 `res.write()` 前都检查 `res.writableEnded`，防止客户端断开连接后写入导致 `ERR_STREAM_WRITE_AFTER_END` 崩溃

**代码实证**：[travel.js#L19-L53](file:///c:/Users/林希/Desktop/ai旅游项目/Backend/src/routes/travel.js#L19-L53)

**面试话术**：
"路由层流式调度的设计我比较满意。Service 层不碰 Response 对象，通过回调函数把数据传出来，保持了 Service 和 HTTP 层的解耦。路由层用三段式 try/catch/finally 确保连接不管成功、失败还是异常都能安全关闭。关键细节是每次写入前检查 res.writableEnded——一开始没加这个，客户端断开后服务端直接崩了，排查发现是 write-after-end 错误。"

---

### 亮点 3：多 LLM Provider 配置切换 — 环境变量驱动的配置隔离

**做了什么**：
- `initLLM()` 通过 `MODEL_PROVIDER` 环境变量判断当前使用的 LLM 服务商
- 根据 Provider 名称读取对应的三组环境变量：`API_KEY`、`BASE_URL`、`MODEL`
- 统一注入 `ChatOpenAI` 构造函数，利用各家兼容 OpenAI 接口协议的特性，通过 `configuration.baseURL` 切换服务商地址
- `streaming: true` 开启流式模式

**代码实证**：[traelService.js#L11-L31](file:///c:/Users/林希/Desktop/ai旅游项目/Backend/src/services/traelService.js#L11-L31)

**面试话术**：
"目前支持 SiliconFlow 和 DeepSeek 两家 LLM 服务商，通过 `.env` 里的 `MODEL_PROVIDER` 环境变量切换。因为各家都兼容 OpenAI 的接口协议，底层统一用 ChatOpenAI 类，改 configuration.baseURL 就能切。改环境变量重启服务即可，不用改业务代码。缺点是必须重启生效，不是热切换。"

---

### 亮点 4：结构化输出容错 — 三层正则兜底解析

**做了什么**：
- 在 Prompt 中嵌入完整 JSON Schema 示例（含 `dailyItinerary`、`budgetBreakdown`、`tips`、`warnings`），严格约束 LLM 输出格式
- 解析层三层正则兜底：
  1. 匹配 ` ```json ``` ` 代码块（最常见情况）
  2. 匹配 ` ``` ``` ` 无语言标记代码块
  3. 匹配裸 `{...}` JSON 对象（兜底）
- 解析失败时打印 LLM 原始输出到控制台，便于排查

**代码实证**：
- Prompt 模板：[traelService.js#L101-L175](file:///c:/Users/林希/Desktop/ai旅游项目/Backend/src/services/traelService.js#L101-L175)（75 行结构化 Prompt，包含 7 个字段的完整 JSON Schema）
- 三层正则解析：[traelService.js#L47-L64](file:///c:/Users/林希/Desktop/ai旅游项目/Backend/src/services/traelService.js#L47-L64)

**面试话术**：
"LLM 的输出本质是非结构化文本，直接 JSON.parse 很容易失败。我做了两层保障：Prompt 层在提示词中嵌入完整的 JSON Schema 约束输出格式，解析层用三层正则兜底——优先匹配 markdown 代码块，其次无标记代码块，最后裸 JSON 对象。这样不管 LLM 具体返回哪种格式都能正确解析。调试时如果解析失败，还会把原始输出打印出来方便排查。"

---

## 四、面试自述模板（3-4 分钟）

> 我做的是一个 AI 智能旅游规划平台，前端 Vue 3 + TypeScript + Vant，后端 Express + LangChain 调大模型。
>
> **先说前端**。我比较注重组件化拆分——把行程详情页拆成 SpotItem、BudgetTable 三个组件，聊天页拆出 ChatBubble 气泡组件，每个组件只通过 props 接收数据，内部用 computed 做 UI 状态派生。路由方面全部用了动态 import 做懒加载，beforeEach 做路由守卫配合前端 Token 认证，App.vue 里登录页隐藏底部导航栏。
>
> 有个架构设计我觉得做得比较好——项目对接了两套 API，我自己的后端和第三方传智 API。我没有把两套 API 的配置混在一起，而是创建了两个独立的 Axios 实例，各有各的 baseURL 和拦截器，第三方 API 的 Token 注入只挂在 itheimaApi 上。
>
> 请求取消也做了——重写 axios.post 挂载 CancelToken，用户返回上一页时主动取消进行中的推荐请求，防止内存泄漏。再加上 300ms 防抖避免重复请求。
>
> **再说后端**。核心是 SSE 流式响应——我把 SSE 协议封装成 createStreamResponse 工具函数，暴露 send/end/error 三个语义化方法。路由层用回调模式接收 LLM 的 token，try/catch/finally 三段式确保连接安全关闭，每次写入前检查 writableEnded。
>
> LLM 接入方面，做了多 Provider 切换（通过环境变量），以及结构化 Prompt 工程——在提示词里嵌入完整 JSON Schema 约束 LLM 输出格式，后端用三层正则兜底解析，保证不管 LLM 返回什么格式都能正确提取 JSON。

---

## 五、面试追问准备

| 问题 | 答案 |
|------|------|
| 为什么没用 Pinia？ | 当前项目状态量不大，用 Vue 3 Composition API 的 ref/reactive 就能管理。如果后期跨组件共享数据变多，会引入 Pinia |
| Vant 全量引入不担心体积？ | 开发阶段为效率全量引入，生产环境应该改为按需引入，可以减小一半以上的包体积 |
| 后端有数据库吗？ | 当前没有，行程数据实时由 LLM 生成不做持久化。后续规划加 Prisma + SQLite |
| 后端 API 有安全防护吗？ | 当前只有 CORS，没有 JWT 中间件和 Rate Limit。正式上线需要加 |
| 最大的技术难点？ | 流式响应全链路调试。涉及 LLM stream、Express SSE、浏览器 ReadableStream 三个环节，特别是 writableEnded 这个坑排查了很久 |
| 这个项目你还想怎么优化？ | 1. Pinia 状态管理 2. 后端 JWT 中间件 + 限流 3. 数据库持久化 4. 组件单元测试 |